// Temporary failing test to verify pre-commit hook blocks commits

describe('Failing Test Suite', () => {
  it('should fail to demonstrate pre-commit hook blocking', () => {
    expect(true).toBe(false);
  });
});
